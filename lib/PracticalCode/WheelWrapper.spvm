package PracticalCode::WheelWrapper {
  use SPVM::Hash;
  use PracticalCode::Vendor::Wheel;

  sub wheel : PracticalCode::Vendor::Wheel ($args : SPVM::Hash) {
    my $wheel = new PracticalCode::Vendor::Wheel(
      $args->get_int{"rim"}, $args->get_float{"tire"});

    return $wheel;
  }

}